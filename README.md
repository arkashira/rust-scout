# Rust Scout

Rust Scout is a tool for ranking Rust crates based on their star trend, performance, and security scores.

## Usage

To use Rust Scout, simply run the `rank_crates` function with a list of crate data.

## Development

To develop Rust Scout, clone the repository and run `poetry install` to install the dependencies. Then, run `pytest` to run the tests.
