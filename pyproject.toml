[project]
name = "rust-scout"
version = "0.1.0"
description = "A tool to curate and search star-trending Rust libraries."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
rust-scout = "rust_scout:main"
